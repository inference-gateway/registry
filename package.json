{
  "name": "registry-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Discovery site for the Inference Gateway Registry (A2A agents and skills).",
  "type": "module",
  "engines": {
    "bun": ">=1.2"
  },
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "codegen": "scripts/codegen-adl.mjs",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^16.0.0",
    "vitepress": "^1.6.4",
    "vue": "^3.5.42"
  },
  "overrides": {
    "vite": "^6.4.3"
  }
}
